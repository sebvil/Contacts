info:
  name: Fetch all contacts
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/contacts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
